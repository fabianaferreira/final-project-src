#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <vector>
#include <dirent.h>
#include <string.h>
#include <fstream>


bool getFilesList(const std::string &, std::vector<std::string> *, const bool);

#endif